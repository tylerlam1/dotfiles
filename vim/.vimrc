"Vim-plug plugin manager
call plug#begin('~/.vim/plugged')

" Vim Embark Theme
Plug 'embark-theme/vim', { 'as': 'embark' }

" Initialize plugin system
call plug#end()

" Initialize VIM color scheme
colorscheme embark

